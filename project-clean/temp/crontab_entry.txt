# Crontab entry for running the update_publication_identifiers.py script
# This will run daily at 2:00 AM
0 2 * * * cd /path/to/DOCiD/backend && ./run_update_identifiers.sh >> /path/to/DOCiD/backend/logs/cron_update_identifiers.log 2>&1

# CSTR identifier update - runs every minute
* * * * * cd /path/to/DOCiD/backend && ./update_all_cstr_identifiers.py >> /path/to/DOCiD/backend/logs/cstr_update.log 2>&1

# Alternatively, use the full path to the Python interpreter in your virtual environment
# * * * * * /path/to/DOCiD/backend/venv/bin/python /path/to/DOCiD/backend/update_all_cstr_identifiers.py >> /path/to/DOCiD/backend/logs/cstr_update.log 2>&1

# Add this to your crontab with:
# crontab -e
# Then paste these lines (adjust paths as needed for your server)
